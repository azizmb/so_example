<%= form_for(@self_service_customer) do |f| %>
  <% if @self_service_customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@self_service_customer.errors.count, "error") %> prohibited this self_service_customer from being saved:</h2>

      <ul>
      <% @self_service_customer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
